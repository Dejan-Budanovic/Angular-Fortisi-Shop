import { ShopDashboardDirective } from './shop-dashboard.directive';

describe('ShopDashboardDirective', () => {
  it('should create an instance', () => {
    const directive = new ShopDashboardDirective();
    expect(directive).toBeTruthy();
  });
});
